def foo(bar, qux):
    print(bar)
    print(qux)

foo(42, 3.141592, 2.718)

# Will get error TypeError: foo() takes 2 positional arguments but 3 were given.
# foo defined with two parameters but passed it three arguments.