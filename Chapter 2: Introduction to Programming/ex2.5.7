def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello')

# Code will raise an error "TypeError: foo() missing 1 required positional argument: 'qux'"
# foo is defined with two parameters but only passed it with one.