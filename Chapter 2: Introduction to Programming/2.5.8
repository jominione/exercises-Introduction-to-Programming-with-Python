# Without running the following code, what do you think it will do?

def foo(bar, qux):
    print(bar)
    print(qux)

foo(42, 3.141592, 2.718)

# Solution: Will raise TypeError because foo accepts two arguments while
# it is being passed three arguments.