# Without running the following code, what do you think it will do?

def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello')

# Solution: Raise an TypeError error as foo function requires two arguments 
# to execute.