# The Following code causes an infinite loop (a loop that never stops
# iterating). Why?

# counter = 0

# while counter < 5:
#    print(counter)

# Solution: counter < 5 always returns truthy value as the body never 
# increment counter.