text = "It's probably pining for the fjords!"

print(text[21:35].rfind('f'))   # 8
print(text.rfind('f', 21, 35))  # 29

# The two prints different values become code in line 3 looks at position of slice of text.
# Line 4 looks at whole string of text between index 21 and 35.